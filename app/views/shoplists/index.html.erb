<div class="row mb-3">
  <div class="col-md-12">
    <h1 class="display-4">
      Shoplists

      <%= link_to new_shoplist_path, class: "btn btn-lg btn-outline-secondary" do %>
        <i class="fas fa-plus fa-fw"></i>
      <% end %>
    </h1>
  </div>
</div>

<!-- Table example -->

<div class="row">
  <div class="col-md-12">
    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>
              Recipe
            </th>

            <th>
              Name
            </th>

            <th>
              Actions
            </th>
          </tr>
        </thead>

        <tbody>
          <%= render collection: @shoplists, partial: "table_row", as: :shoplist %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<!-- Card deck example -->

<% @shoplists.each_slice(3) do |slice| %>
<div class="row">
  <div class="col-md-12">
    <div class="card-deck">
      <%= render collection: slice, partial: "card", as: :shoplist %>
    </div>
  </div>
</div>
<% end %>

<!-- List group example -->

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <div class="card">
      <h1 class="card-header h4 d-flex align-items-center justify-content-between">
        Shoplists

        <%= link_to new_shoplist_path, class: "btn btn-sm btn-outline-secondary" do %>
          <i class="fas fa-plus fa-fw"></i>
        <% end %>
      </h1>

      <div class="list-group list-group-flush">
        <%= render collection: @shoplists, partial: "list_item", as: :shoplist %>
      </div>
    </div>
  </div>
</div>
