<div class="row mb-3">
  <div class="col-md-12">
    <h1 class="display-4">
      Recipe Ingredients

      <%= link_to new_recipe_ingredient_path, class: "btn btn-lg btn-outline-secondary" do %>
        <i class="fas fa-plus fa-fw"></i>
      <% end %>
    </h1>
  </div>
</div>

<!-- Table example -->

<div class="row">
  <div class="col-md-12">
    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>
              Recipe
            </th>

            <th>
              Name
            </th>

            <th>
              Units
            </th>

            <th>
              Quantity
            </th>

            <th>
              Actions
            </th>
          </tr>
        </thead>

        <tbody>
          <%= render collection: @recipe_ingredients, partial: "table_row", as: :recipe_ingredient %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<!-- Card deck example -->

<% @recipe_ingredients.each_slice(3) do |slice| %>
<div class="row">
  <div class="col-md-12">
    <div class="card-deck">
      <%= render collection: slice, partial: "card", as: :recipe_ingredient %>
    </div>
  </div>
</div>
<% end %>

<!-- List group example -->

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <div class="card">
      <h1 class="card-header h4 d-flex align-items-center justify-content-between">
        Recipe Ingredients

        <%= link_to new_recipe_ingredient_path, class: "btn btn-sm btn-outline-secondary" do %>
          <i class="fas fa-plus fa-fw"></i>
        <% end %>
      </h1>

      <div class="list-group list-group-flush">
        <%= render collection: @recipe_ingredients, partial: "list_item", as: :recipe_ingredient %>
      </div>
    </div>
  </div>
</div>
